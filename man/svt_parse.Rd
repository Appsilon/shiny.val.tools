% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{svt_parse}
\alias{svt_parse}
\title{Parse a Shiny app's source.}
\usage{
svt_parse(app_path)
}
\arguments{
\item{app_path}{Path to a Shiny app root or a \code{.zip} archive.}
}
\value{
An \code{svt_parsed} object — a list with \code{app_path}, \code{files}
(relative paths), and \code{asts} (named list, parse() results keyed by
relpath).
}
\description{
Enumerates every R file reachable from the app root (the conventional
starting set plus transitive \code{source()} and \code{box::use()} follow),
parses each retaining srcrefs, and returns the parsed bundle as an
\code{svt_parsed} object.
}
\details{
\code{app_path} may be a directory or a \code{.zip} archive; archives are
extracted to a session-scoped tempdir and the extracted root is used
for all subsequent steps.
}
