% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{svt_scaffold_tests}
\alias{svt_scaffold_tests}
\title{Write test scaffolds for a derived test surface.}
\usage{
svt_scaffold_tests(
  surface,
  out_dir = "validation",
  target = c("staging", "app"),
  features = NULL
)
}
\arguments{
\item{surface}{An \code{svt_test_surface} object.}

\item{out_dir}{The validation directory. Scaffolds are written to
\verb{<out_dir>/tests/} under \code{target = "staging"}.}

\item{target}{\code{"staging"} (default) or \code{"app"}.}

\item{features}{Character vector of surface names to scaffold
(\code{NULL} = all).}
}
\value{
A tibble with one row per scaffold: \code{name}, \code{kind}
(\code{surface} / \code{helpers}), \code{path}, \code{status} (\code{written}, \code{unchanged},
\code{skipped_edited}, \code{skipped_exists}) and \code{warnings}.
}
\description{
One file per surface plus one per helper group. The generated code is
a harness with the plumbing filled in and the assertions left blank:
every \code{test_that()} block opens with a \code{skip()} marker, which is
visible in \code{testthat} output, countable, non-blocking, and detectable
by the coverage classifier. Removing that line is the developer's
explicit act of taking ownership of the test.
}
\details{
Assertions and expected values are never generated — an expected value
is a human judgment about the analysis, and a generated one would be
worse than no test at all.

Overwrite rules. With \code{target = "staging"} files land in
\verb{<out_dir>/tests/}: a scaffold nobody touched refreshes, and one whose
contents have moved away from the artifact manifest's record is a test
somebody wrote and is left alone. With \code{target = "app"} files land in
the app's own \verb{tests/testthat/} and an existing path is \strong{never}
written — it is skipped with SVT-W309.
}
