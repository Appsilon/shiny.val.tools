% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{svt_slice}
\alias{svt_slice}
\title{Slice a graph into feature and module subgraphs.}
\usage{
svt_slice(graph, manifest = NULL, lenient = FALSE)
}
\arguments{
\item{graph}{An \code{svt_graph} from \code{svt_build_graph()}.}

\item{manifest}{Path to \code{features.yml}, an in-memory manifest list,
or \code{NULL} for default slicing.}

\item{lenient}{If \code{TRUE}, manifest issues become warnings rather than
aborts.}
}
\value{
An \code{svt_features} object — a list of feature/module records
with \code{manifest_supplied}, \code{manifest_issues}, \code{unclaimed}, and
\code{app_path} carried alongside.
}
\description{
Without a manifest, the default rule applies (one feature per
top-level output / observer; one module subgraph per \code{moduleServer}
definition). With a manifest, manifest-declared features supersede
default features at the same root, and module subgraphs are still
produced for every module definition.
}
\details{
Manifest validation is run before application; with \code{lenient = FALSE}
(the default) any SVT-W101/W102/W105 issue or a name collision aborts.
With \code{lenient = TRUE} the issues are reported via \code{cli} warnings and
slicing proceeds best-effort.
}
