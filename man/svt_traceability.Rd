% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{svt_traceability}
\alias{svt_traceability}
\title{Write the verification traceability matrix.}
\usage{
svt_traceability(features, coverage, out_dir = "validation")
}
\arguments{
\item{features}{An \code{svt_features} object.}

\item{coverage}{An \code{svt_test_coverage} object.}

\item{out_dir}{The validation directory.}
}
\value{
The two paths written, invisibly.
}
\description{
Emits \code{traceability.json} (\code{schema_version} 1.0) and
\code{traceability.md} — the auditor-facing matrix — into \code{out_dir}. Only
the \verb{## Reviewers} section of the markdown is human-authored; it is
preserved across regenerations like every other stub.
}
\details{
Staleness (SVT-W307) is detected here, because this is the step that
both reads the previous \code{traceability.json} and replaces it: a surface
whose \code{surface_hash} moved while every covering test file's md5 stayed
put is flagged as possibly stale. The check is deliberately
conservative — an md5 change is not evidence the test was updated for
the right reason, only that somebody touched it — so the warning
clears on any edit.
}
