% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{svt_test_coverage}
\alias{svt_test_coverage}
\title{Map an app's existing tests onto its derived test surfaces.}
\usage{
svt_test_coverage(surface, features = NULL, test_path = NULL, results = NULL)
}
\arguments{
\item{surface}{An \code{svt_test_surface} object.}

\item{features}{An \code{svt_features} object, or \code{NULL}. Supplies the
manifest (\code{verification}, \code{rationale_verification}, \verb{tests:}) and
each record's declared risk and intended use.}

\item{test_path}{Where the app's tests live. \code{NULL} = \verb{<app>/tests}.}

\item{results}{Path to a \code{testthat::JunitReporter()} XML report to
ingest, or \code{NULL}.}
}
\value{
An \code{svt_test_coverage} object.
}
\description{
Discovers every \code{test_that()} block under \code{test_path}, links each to
the features, modules and helpers it exercises, and classifies each
surface as \code{covered}, \code{partial}, \code{scaffold}, \code{uncovered} or \code{waived}.
}
\details{
Linking has two mechanisms. A \verb{# @covers feature: km_plot} comment in
the test is the contract; harness-target inference is the convenience
that fills in the rest (a \code{testServer(mod$server, ...)} block resolves
to that module, a \code{testServer()} block over the app directory to the
features whose outputs it reads, a plain block to the helpers it
calls). Which mechanism produced each link is recorded, so a reviewer
can see how much of the matrix is inferred. A test that maps to
nothing is an orphan (SVT-W304); the remedy is an annotation.

Coverage here means \strong{exercised}, never \strong{correct}: a feature
reported \code{covered} has tests that reach its observables; whether those
tests assert the right thing is a reviewer's judgment. Tests are
discovered and mapped, never executed — with \code{results} you can ingest
a \code{testthat::JunitReporter()} XML report your CI already produced, and
each mapped test is stamped \code{pass} / \code{fail} / \code{skip} / \code{missing}.
}
