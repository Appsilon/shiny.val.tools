% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{svt_test_surface}
\alias{svt_test_surface}
\title{Derive the test surface for every feature and module.}
\usage{
svt_test_surface(features, inventory = NULL)
}
\arguments{
\item{features}{An \code{svt_features} object.}

\item{inventory}{An \code{svt_inventory} object, or \code{NULL}. Supplies the
called-function set from which helpers are derived.}
}
\value{
An \code{svt_test_surface} object — a named list of per-subgraph
surface records.
}
\description{
The test surface answers "what is there to test in this feature":
the inputs a test must set (stimuli), the outputs and returned
reactives it can assert on (observables), the intermediates between
them, the trusted terminals, and the app-defined helpers it calls.
Everything is derived from the subgraph — the app is never run.
}
\details{
The package targets \code{testthat} only: every feature and module surface
is driven through \code{\link[shiny:testServer]{shiny::testServer()}}, and each helper gets a plain
\code{testthat} unit stub. Observables whose render function produces a
value that cannot usefully be compared (\code{renderPlot}, \code{renderUI},
\code{downloadHandler}, ...) are annotated \code{opaque} and flagged SVT-W312,
which points the assertion at the helper that computed the data.

Coverage in this layer means \emph{exercised}, never \emph{correct}.
}
