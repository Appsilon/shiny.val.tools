double_it <- function(x) {
  x * 2
}

scale_by <- function(x, factor) {
  x * factor
}

summarise_values <- function(x, factor) {
  paste(x, factor, sep = " / ")
}

ratio_of <- function(x, factor) {
  x / factor
}
